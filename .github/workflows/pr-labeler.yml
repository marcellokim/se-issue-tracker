name: PR 라벨 자동화

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: 변경 경로 기준 라벨 적용
        uses: actions/labeler@v6
